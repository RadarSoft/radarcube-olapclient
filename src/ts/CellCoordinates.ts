namespace RadarSoft {
    export class CellCoordinates {
        row: number;
        col: number;

        constructor(col: number, row: number) {
            this.col = col;
            this.row = row;
        }
    }
}